# SSZ P5 — F3 coupled finite-\ell QNM execution checkpoint
**Date:** 2026-09-15

## 1. F2 handoff correction

The old `37/41` transition label listed
\[
a_5,\quad v_5,\quad c_3,\quad e_3
\]
as unexported lower-order slots.  Only three are independent controls.
Appendix A gives
\[
a_5=a_2'-\frac{A_0''}{2}v_4+\frac{A_0'}{2}(v_5-v_4'),
\]
so once a smooth \(v_5\) member is fixed, \(a_5\) follows from the same
action/background profile.  This does not reopen the F2 constructive
existence closure.

## 2. Production spectral boundary-condition audit

The earlier finite-radius Sommerfeld pole search was not accepted.
It has now been replaced by a high-order outgoing Jost expansion in the
weak exterior.

A transport test of the Jost logarithmic derivative through an exact
Schwarzschild asymptotic tail, from \(R=45\) to \(R=25\), gives errors

- order 4: \(5.28\times10^{-6}\),
- order 6: \(2.08\times10^{-8}\),
- order 8: \(1.80\times10^{-10}\),
- order 10: \(2.43\times10^{-12}\).

Thus the asymptotic series itself is validated.

However, direct inward real-axis QNM shooting is exponentially ill-conditioned
for damped modes: tiny incoming contamination grows while integrating inward.
The candidate complex poles obtained from real-axis Numerov/Jost matching move
significantly when the matching outer radius is changed from 25 to 45.
They are therefore **rejected as physical QNM frequencies** and are not quoted.

The next production complex-pole implementation must use a resonance-stable
formulation such as exterior complex scaling / a continued-fraction or
compactified Jost construction, rather than attempting to stabilize a
fundamentally ill-conditioned real-axis inward shot.

## 3. Geometry-fixed finite-\ell instability screen

For a Maxwell test field
\[
V_\ell^{EM}=f\,\frac{\ell(\ell+1)}{r^2}\ge0
\]
on the entire frozen P5 branch.

For a minimally coupled massless scalar,
\[
V_\ell^{sc}
=
f\left[
\frac{\ell(\ell+1)}{r^2}
+\frac{1}{r\sqrt{f/h}}\frac{d}{dr}\sqrt{fh}
\right].
\]
The numerical global scan finds this potential strictly positive for every
\(\ell\ge1\), including all physical \(\ell\ge2\) ringdown channels.
Consequently these test-field spatial operators have no negative eigenvalue
that could generate a purely exponential \(\omega=i\gamma,\gamma>0\) mode.

This is consistent with the independent WKB result already obtained:
the first trapped scalar ground state appears only near \(\ell=256\), and
the Maxwell threshold is \(\ell=257\).

## 4. Exact coupled HSVT status

A coupled F3 spectrum is more demanding than F2 operator **existence**.
It requires one explicitly frozen lower-order member and one numerical
center-to-infinity reduced operator in a single physical field basis,
including all lower-order terms generated by the radial basis transformation.

The current global `K,R,G` file is a canonical characteristic-basis
principal representative.  The central high-\(L\) mass identities are
action-validated.  Those two artifacts cannot be combined naively into a
finite-\(\ell\) QNM operator because a radius-dependent field transformation
also changes lower-order connection/potential terms.

Therefore the exact coupled HSVT complex spectrum is **not yet honestly
computable from the currently exported numerical artifacts**.

This is now a representation/export blocker, not an action-existence,
light-ring, ghost, gradient, or lower-order-control-rank blocker.

## 5. Current F3 gate

\[
\boxed{\text{F3 test-field low-}\ell\text{ growth screen: PASS}}
\]

\[
\boxed{\text{F3 production outgoing-boundary machinery: Jost tail PASS;\ real-axis pole shot REJECTED}}
\]

\[
\boxed{\text{F3 coupled HSVT QNM spectrum: OPEN ON ONE EXPLICIT FIELD-BASIS }K,R,G,M\text{ MEMBER}}
\]

No \(\operatorname{Im}\omega>0\) claim is made for the coupled theory until
that member has been exported and the complex spectrum has converged under
radial resolution and outer-boundary/resonance-method changes.
