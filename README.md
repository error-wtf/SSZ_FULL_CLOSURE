# SSZ P5 — Full Closure Research Repository

**Carmen Casu and Lino Casu · Anti-Capitalist Software License v1.4**

## Scientific question

Can the SSZ P5 strong-field geometry — including its **inner stable light
ring** (u ≈ 0.706135, W″ ≈ +2.593) — be generated by **one** integrable
covariant Horndeski / U(1)-gauge-invariant scalar-vector-tensor action whose
complete physical perturbation spectrum is ghost-free, gradient-stable,
regular and globally consistent?

**Same-action rule:** background, light rings, perturbations, kinetic
matrices, characteristics, finite-l, odd/vector sectors, interfaces and
QNM must all trace to ONE action member (one `member_hash`). Results from
different historical members are diagnostics, never production evidence.

## Current machine status

The authoritative status is machine-readable, never manual:

```bash
PYTHONPATH=src python -c "
from ssz_p5.closure.gates import full_closure_verdict
print(full_closure_verdict())"
```

As of commit `e087d9d`: **ABSOLUTE_FULL_CLOSURE_PASS = False** —
10 of 28 required gates PASS (fail-closed verdict). Green software tests
are necessary but never equated with physical closure.

## What is established (executable, regression-tested)

- **G10–G12 (Test A/Test B chain)**: the general undivided Eq.-85-generating
  combination is derived from primary sources and machine-verified:
  `C_bg = ALPHA·E00_full + BETA·E22_MH_slice + BETA·Δ22_SVT`
  with `ALPHA = +√f/(r√h)`, `BETA = +r·f^{3/2}/√h`.
  - Test A (G11): the ε_Y deformation is background-null at A0′=0 on the
    same C_bg with f2Y kept symbolic.
  - Test B (G12): the Maxwell–Horndeski projection reproduces the
    **undivided** Kase–Tsujikawa Eq. (85) **exactly** (`== 0` symbolic),
    with symbolic A0′ and the electric term `−2rfhA0′²v8` preserved.
    Permanent slot rule: **MH v8 = ZK V9**, never the canonical V8 column.
  - Negative control: a wrong v8 slot breaks Test B.
- **KT2023 core** (`src/ssz_p5/action/kt_mh_background.py`): verbatim
  transcription of arXiv:2301.10362 Eqs. (7)–(10), (14), (24), (25), (168)
  and the on-shell Eq.-85 certificate; the S0-slice diagonal convention map
  `KT_E00 = −HT_E00/(r²f)`, `KT_E11 = +HT_E11/(r²f)` is verified exactly.
- **Known diagnostic values** (Eq.-85-only, Σ_SVT = 0 only):
  v8(inner ring) = +17.521313 > 0; threshold A0_u² > 0.2230195.
  These are channel diagnostics, never the universal gate.

## Open (the 18 remaining gates)

The fail-closed gate graph (`src/ssz_p5/closure/gates.py`) orders all
remaining work:

```
Δ22_SVT derivation (single symbolic unknown of C_bg)
→ G13 production electric member (f2Y=0, integrated f3X/A0, one hash)
→ G14 full light-ring limit → G15 true Sigma_SVT (operator decomposition)
→ G16 full on-shell LR balance
→ G20 same-member provenance
→ G30–G32 common unreduced H+SVT action, one constraint elimination
→ G40 K > 0 → G50 c_r² > 0 → G60 c_Ω² > 0
→ G70 finite-l even → G71 odd → G72 vector
→ G80 interfaces → G90 global regularity → G100 QNM/trapping (LAST)
```

Gate statuses live only in `GATE_STATUS.json` (absent = NOT_RUN).
`ABSOLUTE_FULL_CLOSURE_PASS` is derived programmatically, never set.

## Repository layout

```
src/ssz_p5/            authoritative implementation
  action/              light-ring identity, C_bg, KT2023 core, canonicalizer
  production/          unsplit SVT background evaluator, emitters
  closure/             fail-closed stage-gate graph
  coefficients/        Appendix-A Maxwell-Horndeski emitter (41 slots)
tests/                 unit / regression / scientific suites
data/generated/        machine-readable scientific evidence
docs/canonical/        frozen derivation protocol + canonical facts
MODEL_LOCK.json        THE one production model/member/convention lock
EVIDENCE_INDEX.json    claim → gate/code/test/evidence/commit
GATE_STATUS.json       machine-written gate statuses
CONTINUATION_STATE.json  operational resume point (next executable command)
```

Historical material (superseded members, forensic audits) remains under
`legacy_*`/`archive`/dated status files and is never a production dependency.

## Reproduce

CPython **3.14**, Linux, from the repository root:

```bash
git clone https://github.com/error-wtf/SSZ_FULL_CLOSURE.git
cd SSZ_FULL_CLOSURE
python3.14 -m venv .venv && source .venv/bin/activate
python -m pip install -r requirements.lock
python -m pip install --no-deps -e .
pytest -q                      # full suite (fail-closed provenance included)
```

Symbolic closure layers (fast, deterministic):

```bash
PYTHONPATH=src pytest tests/regression/test_light_ring_identity.py -v
PYTHONPATH=src pytest tests/regression/test_kt_mh_background.py -v
```

## Sources

Primary: Kase & Tsujikawa, arXiv:2301.10362 (PRD 107, 104045 (2023)) —
Eq. (85) identity, Appendix A slots; Heisenberg & Tsujikawa,
arXiv:1802.07035 (PLB 780, 638 (2018)) — background EOMs (14)–(20).

## Policy

No fitting. No member switching. No weakening of justified tolerances.
A scientific failure with full provenance is preferable to fake closure.
See `docs/canonical/NEXT_BLOCK_ZK_LR_DERIVATION.md` (frozen protocol) and
`CONTINUATION_STATE.json` (exact next executable command).
