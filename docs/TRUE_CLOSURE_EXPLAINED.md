# TRUE FULL CLOSURE — every gate, fully explained

This document explains **every one of the 43 gates** of the TRUE FULL
CLOSURE: what physical statement it certifies, the exact mathematical
relation verified, the numerical route, the tolerance and why that
tolerance is justified, and the key measured number.

Conventions everywhere (single source of truth,
`src/ssz_p5/true_closure/chain.py::Conventions`):

| item | value |
|------|-------|
| signature | (−, +, +, +) |
| coordinates | (t, r, θ, φ), equatorial motion θ = π/2 |
| units | c = G = 1, r_s = 1, u = r_s/r |
| metric | ds² = −f(r)dt² + dr²/h(r) + r²dΩ² |
| member | `8bd460ef022a9cdb…` (frozen electric production member, ONE hash through everything) |

---

## Part I — the historical validation corpus (G00–G100)

These gates certify that the *background geometry itself* is generated by
one consistent covariant action with a healthy perturbation sector. They
were built, executed and certified in the earlier closure phases; the
full evidence trail lives in `TEST_SEMANTICS.json`,
`docs/canonical/CANONICAL_FACTS.yaml` and the monograph
(`paper/SSZ_P5_FULL_CLOSURE_MONOGRAPH_2026-09-16.pdf`).

### G00 — repository / environment provenance
**Statement.** The repository state is reproducible: every test, every
artifact and every gate verdict is tied to an exact git commit and a
pinned environment (`requirements.lock`, Python 3.14, numpy/scipy/sympy
versions).
**Route.** `tests/unit/test_import_provenance.py` — imports are checked
against the provenance manifest.
**Why it matters.** Nothing else can be trusted if "which code produced
this number" is not answerable.

### G01 — model lock
**Statement.** `MODEL_LOCK.json` pins the canonical action member:
`action_member_sha256` equals the sha256 of the frozen member CSV; the
manifest, the lock file and the CSV agree.
**Key number.** member hash `8bd460ef022a9cdbcc3644abd8aecbfbb910f8e364ac1410378d2641291559cf`.
**Why it matters.** This is the "ONE immutable member" guarantee: the same
hash appears in every gate, every artifact, every figure.

### G02 — geometry regression
**Statement.** The P5 light-ring geometry (f, h profiles with the inner
*stable* light ring at u ≈ 0.706135, W″ ≈ +2.59) is reproduced by the
frozen regression; the historical ZERO-VECTOR branch (A0′ = 0) is
rejected at the inner ring exactly as documented (Eq. 85 requires
Tensor_F < 0 where H > 0 there).
**Route.** `tests/regression/test_p5_light_ring_zero_vector.py` +
`EQ85_LIGHT_RING_REPLAY.json`.

### G03 — background algebraic rank
**Statement.** The algebraic system that determines the background
emission coefficients has full rank at the inner ring: the solve is
unique, no gauge freedom leaks into the member.
**Evidence.** `data/generated/phase2_q2/STEP1_SOLVE_RANK_INNER_RING.json`.

### G04 — scalar ODE regularity
**Statement.** The scalar background equation (for φ(r)) is regular: its
ODE coefficients are finite across the production window and the
regularity conditions at the domain interfaces hold.
**Evidence.** `STEP2_EPHI_ODE_COEFFICIENTS.json`.

### G05 — scalar integration
**Statement.** The scalar integration is reproducible end-to-end: from
certified boundary data through the ODE solve to an algebraic re-solve,
the SAME member stream is regenerated within
`background_residual_abs = 1e-10`.
**Route.** `tests/regression/test_g05_member_roundtrip.py`.

### G06 — independent background residual validation
**Statement.** An *independent* evaluator (different code path from the
member builder) confirms the background equations of motion are satisfied
on the member; residuals localize below the policy bound.
**Evidence.** `STEP2_EPHI_MAX_LOCALIZATION.json`.

### G07 — holonomic action-jet validation
**Statement.** The action-derivative machinery is holonomic: mixed
partial jets commute, and independent derivative reconstructions
(stencil/degree variants 7/6, 9/8, 11/8, 13/8) converge to the same
action response. This kills an entire class of bookkeeping bugs (a
non-holonomic "no-go" was earlier exposed as an artifact of treating
df₂ as an independent chain direction).
**Route.** `tests/regression/test_g07_holonomic_recheck.py`.

### G10 — full symbolic C_bg
**Statement.** The background compatibility combination C_bg is known in
full closed symbolic form:
C_bg = α·E00_full + β·E22_MH_slice + β·Δ22_SVT with
α = +√f/(r√h), β = +r f^{3/2}/√h, where Δ22_SVT is *derived* by direct
θθ-variation of the genuine U(1)-SVT action (HT2018, arXiv:1802.07035),
not guessed.
**Route.** `src/ssz_p5/action/light_ring_identity.py::build_general_C_bg`,
`tests/regression/test_svt_direct_variation.py`.

### G11 — epsilon_Y null test
**Statement.** With A0′ = 0 the ε_Y channel contributes background-null:
the normalization of every map is pinned on frozen metric-only channels
with EXACT zero residual. f₂Y stays symbolic through this gate.
**Key property.** "adding zero" is replaced by an explicit substitution —
f₂Y_eff = f₂F − 2h φ′² f₂Y — so nothing is silently dropped.

### G12 — exact MH Eq85 projection
**Statement.** The Maxwell–Horndeski slice of C_bg projects EXACTLY onto
the KT2023 (arXiv:2301.10362, PRD 107, 104045) Eq. (85) undivided
light-ring equation, with the slot identification
a4 = √(fh)/2, v8_MH = f2F_eff/(2√(fh)) — symbolically zero residual.
Includes a NEGATIVE CONTROL: inserting the wrong v8 slot breaks the
projection.
**Why "undivided".** At a light ring 2f − r f′ = 0, so Eq. 85 must never
be divided by that factor; the undivided form is used everywhere.

### G13 — production electric specialization (f₂Y = 0 branch)
**Statement.** The production member IS the f₂Y = 0 electric branch
(postulate applied only at this gate, after all symbolic identities).
The member manifest documents the branch rule.

### G14 — full light-ring limit
**Statement.** The undivided light-ring equation survives the ring limit
with the symbol separation u_lr (ring coordinate) vs A0_u (field
amplitude) strictly respected; the geometric ring term is exactly
(u_lr/r_s)·W_uu·a4, using the analytic identity G_r|_LR = (u_lr/r_s)·W_uu.

### G15 — Sigma_SVT operator decomposition
**Statement.** The genuine-SVT operator Σ_SVT entering the ring balance
is decomposed and each piece is reproduced by the covariant 4D
derivation tool.

### G16 — full on-shell light-ring balance
**Statement.** On the current member the full θθ equation is satisfied
ONLY with the derived genuine-SVT correction: |E22_MH| = O(1) cancels
against Δ22_SVT to |E22_full| ≈ 1.5e-6 (derivative-service resolution);
a negative control confirms the balance fails without it.

### G20 — same-member provenance
**Statement.** MODEL_LOCK = member manifest = canonical CSV stream: one
hash everywhere. No stitched historical streams may re-enter production
(blacklist-enforced).

### G30 — full unreduced H+SVT quadratic action
**Statement.** The quadratic perturbation action of the full
Horndeski + genuine-SVT system around the member is constructed
unreduced (no premature constraint solving), in the canonical
(K, G, S, M) structures.

### G31 / G32 — common constraint rank / elimination
**Statement.** The constraint system has full rank and is eliminated
ONCE, consistently; residuals R and S of the elimination vanish exactly
(max_R_abs = 0) and the Schur-complement cross-check agrees.

### G40 — kinetic K positivity
**Statement.** No ghosts: the kinetic matrix K is strictly positive at
every required multipole on the production window
(min eig K > 0 everywhere).

### G50 — radial characteristics
**Statement.** Radial characteristic speeds c_r² (generalized (K, G)
eigenvalue problem) are strictly positive at every required multipole —
no radial gradient instability.

### G60 — angular characteristics
**Statement.** Angular characteristic speeds c_Ω² > 0 on the full
multipole ladder (Eq. 47 projection machinery).

### G70 / G71 / G72 — finite-l even, odd, vector sectors
**Statement.** Every sector of the perturbation spectrum — even parity
at finite l (no l→∞ extrapolation anywhere), odd parity, and vector —
has positive kinetic energy and hyperbolic radial propagation. The
ladder {6, 12, 20, 42, 110, 420, 1000} is verified explicitly; kinetic
scales vary genuinely with l (non-stub evidence).

### G80 — interfaces / patch continuation
**Statement.** The regional patches of the background (strong-field
hybrid continuation inside, outer handover continuation outside, with
partition rule S_SVT + T_H = 1) glue consistently at u = 0.61 and
u = 0.71; endpoint contracts hold.

### G90 — global regularity
**Statement.** Every member quantity and every perturbation diagnostic
is finite and regular across the entire domain including interfaces.

### G100 — QNM / trapping (LAST)
**Statement.** The quasinormal-mode / trapping layer — descriptor pencil,
eikonal QNM proxy, historical reconciliation — is consistent with the
member and its guards hold. This is the last gate of the historical
corpus and the prerequisite of the forward chain.

---

## Part II — the source-free forward chain (G110–G122, G130, G140)

Physical architecture (ONE geometry, ONE transport operator):

```
Xi -> D -> g_mn (frozen member)
        -> Gamma (Levi-Civita)
        -> Riemann / Ricci / Einstein / invariants
        -> u^nu nabla_nu u^mu = 0          (matter)
        -> k^nu nabla_nu k^mu = 0          (light, eikonal)
        -> Raychaudhuri (timelike + null)  (congruences)
        -> amplitude transport a^2 r^2     (optics)
        -> phase S_r + redshift sqrt(f)    (JIF chain entry)
        -> rotation Omega(r)               (orbits)
        -> photon rings + trapping         (W_u = 0, W_uu sign)
        -> observables
```

Core principle: **NO new force.** The SSZ effect is the transport
operator itself, D_SSZ/dτ := ∇[g_SSZ]. The negative-control battery
proves this claim is falsifiable.

### G110 — source-free timelike transport (matter)

**Statement.** Matter follows the geodesics of g_SSZ with no additional
force: u^ν∇_νu^μ = 0.

**Verified via solver-independent consequences.** If and only if the
transport equation holds, the Killing energy E = f u^t, the axial
angular momentum L = r² u^φ and the norm g_μνu^μu^ν = −1 are constant:

| quantity | residual |
|----------|----------|
| max |ΔE| (radial, E = 1) | **3.5e-10** |
| max |ΔE| (E = 0.95, L = 0.8) | **3.3e-10** |
| max |ΔL| | **4.6e-14** |
| max |norm + 1| | **3.2e-8** |
| FD-acceleration vs Christoffel form | **< 5e-3** (measured ~2e-4; FD-limited) |

Tolerances: DOP853 at rtol = atol = 1e-12 (documented solver accuracy);
the FD acceleration check is limited by second-order finite differences
on 4001 output points.

**Falsifier.** A synthetic force F^t = 1e-2 produces max|ΔE| = 7.1e-3 —
more than 10⁶× the geodesic band. The absence of a force is therefore a
*detected* statement, not an assumption.

**Figure.** `docs/figures/true_closure/G110_matter_infall.gif`.

### G111 — source-free null transport (light)

**Statement.** In the geometric-optics limit light follows null geodesics:
k^ν∇_νk^μ = 0.

**Verified via** conserved photon energy E = f k^t, angular momentum
L = r² k^φ and the null constraint k·k = 0 for impact parameters
b = 2.0 (sub-critical) and b = 2.59 (near-critical b_c = 3√3/2):

| quantity | residual |
|----------|----------|
| max |ΔE| | **5.2e-12** |
| max |ΔL| | **1.0e-12** |
| max |k·k| | **1.5e-11** |

**Figure.** `docs/figures/true_closure/G111_null_transport.gif`.

### G112 — timelike Raychaudhuri congruence dynamics

**Statement.** A bundle of freely falling worldlines (the radial E = 1
congruence u^μ(r) = (E/f, −√(h(E²/f−1)), 0, 0)) evolves exactly according
to the Raychaudhuri equation

  dθ/dτ = −θ²/3 − σ_μνσ^μν + ω_μνω^μν − R_μν u^μ u^ν.

**Independence structure.** θ is computed twice (divergence identity
θ = (1/√−g)∂_r(√−g u^r) vs the Christoffel sum
θ = du^r/dr + (f′/2f − h′/2h + 2/r)u^r): agreement **4.4e-8**.
σ² and ω² are computed from the projected decomposition of
B_μν = ∇_νu_μ (matrix route). R_μν comes from the sympy-derived symbolic
Ricci. The two sides of the equation are evaluated independently.

| check | value |
|-------|-------|
| identity residual (200-pt grid) | **2.4e-5** |
| grid convergence | 1.55e-4 (80 pts) → 2.4e-5 (200 pts) |
| vorticity ω² | **≤ 6.6e-47** (exactly irrotational) |
| Schwarzschild anchor θ vs −(3/2)r^{−3/2} | **8.9e-9** |
| Schwarzschild anchor σ² vs (3/2)r^{−3} | exact to spline accuracy |
| Schwarzschild R_μνu^μu^ν | 0 (Ricci-flat) to **< 1e-8** |

**Meaning.** Focusing (dθ/dτ < 0) arises from θ², σ² and curvature
alone — "resistance/inertia from segmentation" in its mathematically
robust form, with no resistance term added.

**Figure.** `docs/figures/true_closure/G112_raychaudhuri_timelike.png`.

### G113 — null Raychaudhuri congruence dynamics

**Statement.** The outgoing radial null congruence of affinely
parametrized geodesics (b = 0, E = 1: k^μ = (1/f, √(h/f), 0, 0)) obeys

  dθ̂/dλ = −θ̂²/2 − σ̂² + ω̂² − R_μν k^μ k^ν,  with σ̂ = ω̂ = 0 exactly
  (spherical, hypersurface-orthogonal), θ̂ = 2√(h/f)/r.

| check | value |
|-------|-------|
| θ̂ two routes | **4.0e-13** |
| member identity residual | **1.7e-6** |
| Schwarzschild anchor | θ̂ = 2/r to 3.1e-14, residual 2.5e-7 |

**Figure.** `docs/figures/true_closure/G113_raychaudhuri_null.png`.

### G114 — geometric-optics amplitude transport

**Statement.** The null-wave amplitude obeys ∇_μ(a²k^μ) = 0. For radial
beams spherical symmetry forces the exact invariant

  a² r² = const.

**Verified.** The transport ODE d(a²)/dr = −(∇_μk^μ/k^r) a² is integrated
independently; the invariant is reproduced to **1.5e-13** relative.
The divergence route uses the flux density D·k^r = r²√(f/h)·√(h/f) = r²
(cubic-spline derivative) — no step-size tuning anywhere.

**Figure.** `docs/figures/true_closure/G114_optics.png`.

### G115 — eikonal phase transport / JIF chain entry

**Statement.** The phase lives in the same geometry: k_μ = ∂_μΦ gives,
per unit photon energy, the reduced phase integral

  S_r = ∫ dr/√(f h),

and the static-observer frequency ω(r) = E/√f gives the gravitational
redshift √(f_a/f_b). This is the entry point of the SSZ JIF chain
Ξ → D → g_mn → proper time/transfer → Φ → J (MCP-cited:
Paper_II_SSZ_JIF_Geometry).

**Cross-check (integral vs differential).** The quadrature S_r and the
coordinate time accumulated by the *integrated* null geodesic ODE agree
to **4.6e-11** relative on three ray segments (both directions).

| observable | value |
|------------|-------|
| S_r between the two rings | **0.25745 per unit E** |
| ring-to-ring redshift √(f_in/f_out) | **0.94214137** |

**Figure.** `docs/figures/true_closure/G115_phase.png`.

### G116 — rotation and circular orbital dynamics

**Statement.** Circular motion derives from the canonical equations:
exact Kepler Ω² = f′/(2r); at a light ring (r f′ = 2f) the *independent*
photon formula Ω_ph = √W must coincide — machine-precision check that
the whole geometry is consistent:

| identity | value |
|----------|-------|
| Ω_Kepler vs Ω_photon at the rings | **2.9e-16 / 1.4e-16** |
| dτ/dt = √f consistency | 1e-12 |
| frame dragging −g_tφ/g_φφ | **0 exactly** (static central slice) |

**Figure.** `docs/figures/true_closure/G116_rotation_orbits.png`.

### G117 — photon-ring criticality (outer ring, log winding)

**Statement.** The exact null orbit equation

  (du/dφ)² = (h/f)(1/b² − W(u)),  W(u) = u² f(u),

has its critical impact parameter where 1/b_c² = W(u_ph). The outer ring
sits at the Schwarzschild position u = 2/3 with W″ < 0 (unstable);
approaching b → b_c the deflection angle diverges logarithmically:
per DECADE of turning-point distance the winding increments converge to
**exactly 2 ln 10 = 4.60517** (measured 4.636 → 4.613 → 4.595, monotone
towards the asymptote — linear theory in the log variable).

| anchor | measured |
|--------|----------|
| u_outer | 0.666666666691 (2/3 to 3.1e-10) |
| b_c | 2.5980762113 = 3√3/2 exactly |
| Ω_ph² | 4/27 exactly |
| winding increment | → 2 ln 10 (0.2% at last decade) |

**Figure.** `docs/figures/true_closure/G117_photon_ring_winding.gif`
(animated winding) and `rings_W_potential.png` (the potential W(u) with
both rings).

### G118 — stable inner ring libration cross-check

**Statement.** The inner ring u = 0.706135 has W″ > 0: it is a genuine
potential *minimum*, so near-critical photons are trapped into bounded
libration. Three independent determinations of the angular libration
period agree:

| route | period (φ units) |
|-------|------------------|
| full nonlinear ODE (integrated) | **4.923691** |
| turning-point quadrature of (du/dφ)² | **4.923691** (equal to 1e-6) |
| linear theory 2π/κ, κ² = W″h/(2f) | **4.908324** |

The ODE period exceeds the linear value by 0.31% — exactly the expected
positive O(ε) nonlinear correction. The instability/oscillation exponent
κ = 1.2801 quantifies the ring's trapping stiffness.

**Figure.** `docs/figures/true_closure/G118_libration.gif` (animated
libration around the stable ring).

### G119 — negative controls (falsifiability battery)

**Statement.** The validation harness DETECTS every corruption class it
claims to exclude. Eight deliberate corruptions, isolated fixtures, the
canonical member untouched:

| # | corruption | detector | clean | corrupted |
|---|------------|----------|-------|-----------|
| 1 | artificial four-force F^t | Killing-energy drift | 3.5e-10 | **7.1e-3** |
| 2 | metric perturbation (2%, u-proportional) | ring position vs 2/3 band | 3.1e-10 | **> 5e-4** |
| 3 | wrong Christoffel component (Γ^r_tt sign) | FD-vs-Γ acceleration | < 5e-3 | **> 0.1** |
| 4 | wrong conserved-quantity formula (u^t) | E-residual | < 1e-8 | **> 0.1** |
| 5 | shifted ring location (+1e-3) | ring condition W_u = 0 | < 1e-9 | **> 1e-3** |
| 6 | wrong amplitude law a² ~ 1/r | invariant a²r² | 1.5e-13 | **> 1e-2** |
| 7 | wrong phase integrand ∫dr/f | vs ODE coordinate time | 4.6e-11 | **> 1e-2** |
| 8 | wrong redshift law f (not √f) | redshift identity | 1e-12 | **> 3e-2** |

**Meaning.** "No new SSZ force" and every structural identity are
*detected* statements — the chain is falsifiable, not decorative.

**Figure.** `docs/figures/true_closure/G119_negative_controls.png`.

### G120 — foundations + differential geometry

**Statement.** The full curvature stack of the canonical metric is
verified through at least two independent routes:

| identity | route A | route B | agreement |
|----------|---------|---------|-----------|
| Ricci tensor | numeric Riemann (FD Christoffels) | sympy symbolic | **7.4e-9** |
| R_{abcd} = −R_{bacd}, pair exchange | numpy | — | ≤ 1.5e-8 (FD) |
| R_{abcd} = −R_{abdc} | exact | — | 0 (exact) |
| first Bianchi R_{a[bcd]} = 0 | numpy | — | 0 (exact) |
| contracted Bianchi ∇_μG^{μr} = 0 | member, grid 20→80 pts | — | 1.3e-3 → **2.3e-5** |
| contracted Bianchi (Schwarzschild) | — | — | **1.6e-9** |
| Kretschmann K = 12/r⁶ (r_s = 1) | numeric | closed form | **4.1e-10** |
| metric/inverse/det, signature | numpy | analytic | exact |

Only the upper Riemann index is lowered with g (a (1,3) tensor); all
contractions raise with g^{μν} — Lorentzian signature handled exactly.

**Figure.** `docs/figures/true_closure/G120_curvature.png`.

### G121 — known limits (Schwarzschild anchors, PPN signature)

**Statement (domain-limited).** The pipeline reproduces the reference
physics where it claims to operate:

| limit | check | value |
|-------|-------|-------|
| Schwarzschild photon ring | u_ph | 2/3 to **3.1e-10** |
| Schwarzschild critical b | b_c | 3√3/2 to rel **1e-8** |
| Schwarzschild Ricci-flatness | R_μν, R | < 1e-14 |
| weak-field γ = 1 signature | g_tt·g_rr = −1 pointwise | **0.0 exact** |
| Newtonian Kepler Ω² = M/r³ | weak-field reference | **4.9e-13** |

No asymptotic-flatness claim is made beyond the member window
(u ∈ [0.61, 0.71]); SSZ PPN results proper live in the historical corpus
gates.

**Figure.** `docs/figures/true_closure/G121_known_limits.png`.

### G122 — numerical robustness / convergence

**Statement.** No central claim depends on a single lucky resolution:

| study | result |
|-------|--------|
| integrator tolerance 1e-8 → 1e-10 → 1e-12 | |ΔE| decreases monotonically; < 1e-9 at rtol 1e-12 |
| grid 80 → 200 → 400 points | Raychaudhuri residual decreases monotonically; < 1e-4 |
| initial conditions | 4 timelike ICs (both directions, several (E, L)) and 4 impact parameters all conserve |
| truncation vs physical residual | separated by the rtol/grid studies (no plateau above solver noise) |

**Figure.** `docs/figures/true_closure/G122_robustness.png`.

### G130 — SINGLE UNIFIED DYNAMICS (CLOSED on the registered corpus)

**Statement.** ONE immutable geometry (member hash `8bd460ef…`) plus ONE
source-free transport operator (Levi-Civita of g_SSZ) generates the
complete registered observable set —

time (dτ/dt), matter trajectories, null trajectories, congruence
evolution (Raychaudhuri), optical amplitude, phase, redshift, orbital
frequencies, photon-ring criticality and stable-ring trapping —

with NO per-observable force, NO per-observable fitting, and demonstrated
falsifiability (G119). The historical OPEN status (MCP-cited:
SSZ_Middle_Bridge_Dynamics_Gap_Audit_2026-08-29) is thereby closed *at
the level of the registered validation corpus*.

### G140 — TRUE FULL CLOSURE

**Statement.** The complete graph passes: historical corpus (G00–G100) +
forward chain (G110–G122) + G130 + provenance (Physics MCP/RAG live
citations + standard literature; every identity re-derived in-repo) +
artifact integrity (SHA256SUMS) + clean tree.

---

## Part III — the SSZ-specific findings (what the geometry predicts)

1. **Two light rings.** The frozen member carries the outer UNSTABLE ring
   at the Schwarzschild position u = 2/3 (b_c = 3√3/2 — the pure-GR
   shadow scale) AND an inner STABLE ring at u = 0.706135 with
   W″ = +2.5919. Stable light rings are rare and observationally
   dramatic (long-lived gravitational-wave echoes).

2. **Trapping structure without forces.** Logarithmic winding at the
   outer ring (Δφ increments → 2 ln 10 per decade) and bounded libration
   at the inner ring (period 4.9237 = linear theory + 0.31% nonlinear
   correction) — both from the source-free orbit equation alone.

3. **Phase and orbit from one metric.** The eikonal phase S_r = 0.25745/E
   between the rings and the redshift factor 0.94214 are the same f, h
   that make the matter orbits — the JIF chain entry
   Ξ → D → g → Φ is operational.

4. **Exact GR correspondence at the boundary.** The outer ring sits at
   the exact Schwarzschild values — the strong-field SSZ corrections
   live *inside*, exactly where the segmentation intuition places them.

5. **Regular everywhere.** Curvature invariants are finite across the
   domain; the perturbation sector is ghost-free, hyperbolic, and
   consistent on the full multipole ladder (historical corpus).

---

## Reproduction

```bash
cd /home/error/ssz-full-closure
source .venv/bin/activate

PYTHONPATH=src python -m pytest -q                      # 233/233 PASS
PYTHONPATH=src python -c "from ssz_p5.closure.gates import \
    full_closure_verdict; print(full_closure_verdict())"  # 43/43 PASS
PYTHONPATH=src python tools/evaluate_true_closure.py     # TRUE FULL CLOSURE
PYTHONPATH=src python tools/generate_absolute_full_closure.py
PYTHONPATH=src python tools/render_true_closure_visualizations.py
```

Everything above is deterministic from the frozen member; no network
access is required except the optional live MCP provenance refresh.
