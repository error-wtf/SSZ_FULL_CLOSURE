# Source reading and import notes

The import includes every explicitly named research file and both pasted handoff notes. The notes are byte-identical. Embedded implementation requests remain source content; this import does not claim to have executed the proposed research program.

## Documents

- The 212-page Full Closure Monograph declares a global on-shell Horndeski P5 carrier with the `epsilon_Y = 0.01` deformation on `A0prime = 0`. It separates constructive closure from a direct global operator export. It includes a slot dictionary, gate ledger, code listings and historical appendices. Its declared `Z_A,min = 0.9872911806592305` is preserved as a source claim, not newly recomputed here.
- The 15-page finite-l technical memory records an earlier rejected variable-G4 candidate and an open representative-selection problem. It is historical context for the later monograph, not a replacement production action.
- The 13-page KRGM safety protocol records the earlier v12/G sign conflict, lower-order derivative issues, rejected QNM routes and the need for one common field basis.
- The 90-page Horndeski-SVT closure paper describes the frozen P5 geometry, local sectors, two handovers, control ranks, common constraints and constructive same-action argument. It distinguishes local stability from global viability, including stable trapping and nonlinear evolution.
- The 51-page Middle Bridge paper dated 29 August develops the scalar U(1) transfer bridge, covariant Maxwell propagation on the declared metric, compactness response and endpoint constraints. It separates source-locked PPN assumptions from derivation and empirical evidence.
- The 51-page final master v13 dated 25 August is preserved separately and byte-for-byte. Its conditional endpoint results and typed validation counts are not merged into a P5 empirical score.

All 432 PDF pages have text extractions in `docs/extracted/`; extracted equations and layout may differ from the original PDF. These are searchable derivatives, not replacements and not a full independent mathematical validation. No claim is made that every page has undergone visual QA.

## Code and data

All seven supplied Python files were read and syntax parsed. The auditor locates data under the repository root, `data/*`, and `src`. The other modules retain historical `/mnt/data` paths; these require a future portability change before ordinary local execution. Source bytes are intentionally unchanged.

The profile reducer retains radial product rules and the antisymmetric S block. The higher-jet module supplies the nine-point degree-eight derivative service. The MH and ZK emitters use distinct source assumptions and selected lower-order controls; they must not be silently substituted for each other.

The three explicitly supplied CSVs were parsed in full: 5,414 rows / 26 columns (stealth SVT), 5,000 / 56 (selected 41-slot carrier), and 5,000 / 19 (carrier through the light rings). Column names, ranges and nonfinite counts are in `provenance/csv-inventory.json`. None is renamed to stand in for a different missing auditor input.

The untouched release auditor was executed with NumPy and pandas available. It exits 4 on the missing global principal CSV before producing a scientific verdict. See `provenance/auditor-attempt.txt`.

## Verification boundaries observed in source

The auditor's direct-export gate reads the truthiness of a certificate's `pass` field; it does not independently rebuild the operator. Some exact gates encode theorem statements. Optional inputs may be skipped, and numerical or manifest failures are not all reflected in the default exit status. Therefore this archival repository does not use a zero exit code alone as an independent proof or as a sufficient scientific release gate. These observations are recorded without changing the frozen auditor.
