# Superseded / diagnostic material

Do not use the following historical classes of artifacts as production truth:

- the Sep-16 variable-G4 `OUTER_FINAL_SAME_ACTION*` transplant candidate;
- any table using `v12=+v6/(2h)`;
- any lower-order completion omitting the `-a1''` term in a5;
- the experimental central A2/v6 ODE repair files created while a split-SVT table was temporarily treated as an unsplit full action;
- naive inner `E_H + E_SVTg` residual columns as a full background residual;
- deep-core direct K eigenvalues from rounded historical coefficient dumps without action-level regeneration.

Keep these only for provenance and regression against past mistakes.
